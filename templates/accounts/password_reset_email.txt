Hello {{ user.get_full_name|default:user.username }},

Somebody asked to reset the password on your Smart Queue account. If that was
you, set a new one here:

{{ protocol }}://{{ domain }}{% url 'accounts:password_reset_confirm' uidb64=uid token=token %}

The link works once and stops working three hours after it was sent.

If you did not ask for this, you can ignore this email. Your password stays as
it is, and nobody is told you received this.

-- Smart Queue
